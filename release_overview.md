## 🚀 Release v1.0.0 Overview

### 📦 Completed Tasks & Deliverables:

- **T1–T3:** Core CRUD for Events, Categories, Registrations & JWT Auth.
- **T4:** Safe whitelist sorting by `date` and `registrations` count (`GET /api/v1/events`).
- **T5:** Real-time announcements via Socket.io with JWT handshake auth & room join validation.
- **T6:** Centralized `errorHandler` (ValidationError, CastError, 11000, AppError) & Jest test suite (14 passing tests).
- **T7:** OpenAPI / Swagger interactive documentation at `/api-docs`.
- **T8:** Complete Postman collection with `:id` path variables & saved responses in `postman/`.
- **T9:** Cleaned code, `.env.example`, and annotated release tag `v1.0.0`.

### 🧪 Verification:

- `npm test` passing 14/14 tests across 3 suites.
